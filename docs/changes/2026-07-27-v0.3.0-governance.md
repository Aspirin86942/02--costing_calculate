# v0.3.0 版本、文档与发布治理

- 日期：2026-07-27
- 状态：已完成

## 实际结果

- Rust workspace、Cargo lock、应用版本测试、Manifest golden、Python 验证环境和发布示例统一为 `0.3.0`。
- `RunManifestV1` schema 保持 V1，字段没有随应用版本改变。
- release workflow 从 Cargo metadata 读取 `costing-calculate` 版本，只接受对应的 `v<version>` 或 `v<version>-rc.N` 标签，并继续校验标签指向当前 commit。
- Windows package / smoke 的 RC 标签规则统一为从 `rc.1` 开始。
- 根 README 只保留安装、运行、输入输出、常见错误和发布包说明。
- 新增当前架构和工作簿/运行契约文档；项目提示词同步 Rust/Python 边界、模块依赖、高风险业务不变量和四类文档写入规则。
- `docs/README.md` 成为统一导航；Python 退役计划标记为 Completed 并链接实际 change。
- CI 新增本地 Markdown 链接门禁。
- 新增 normal-mode N=5 wall、Peak Working Set 和输出大小测量入口。

## 验证

- `costing-calculate --version` 输出 `0.3.0`。
- 确定性 `--version-json` build identity 校验通过。
- Rust fmt、全 workspace/全 target/全 feature Clippy 和全 workspace/全 feature测试通过。
- Python Ruff lint / format、Markdown 链接和精简测试通过：`62 passed`。
- 合成 GB/SK 配置、check-only、正式 workbook 和禁止覆盖 smoke 通过。
- 性能脚本用合成 GB 输入完成一次进程级 wall/PWS/输出测量，报告结构和阈值执行正常。
- `docs/superpowers/` 未修改。

## 剩余风险

本阶段不代表 RC 已验收。真实 GB/SK 最终等价比较、N=5 性能、Windows ZIP 和无 Rust/Python child `PATH` smoke 在下一阶段执行。
