# v0.3.0 Python 旧实现退役

- 日期：2026-07-27
- 状态：已完成

## 实际结果

- 工作簿 XML/语义比较和 GB/SK 合成输入生成器已先移动到 `tools/validation/`。
- 新增统一跨版本命令 `python -m tools.validation.compare_releases`，输入基线 binary、候选 binary、管线和 workbook，输出不含单元格值与敏感路径的结构化报告。
- 删除 `main.py`、`src/` Python 业务实现、只验证 Python 旧实现的测试、Phase 0/meta harness 与 `_archive/gui-removed-20260709/`。
- CI、合成验收和发布输入生成不再导入 `tests.rust_oracle`。
- Python 环境改为非打包的验证工具环境，只保留 `openpyxl`、`xlsxwriter`、pytest 和 Ruff；移除 pandas、NumPy、Polars、python-calamine、BeautifulSoup 及 setuptools 构建配置。
- 删除 `meta` 测试分类；真实数据和性能仍作为显式慢速门禁。

## 验证

- Ruff lint 与 format check 通过。
- 精简后的公共测试：`59 passed`。
- 合成 GB/SK 配置、check-only、正式 workbook 和禁止覆盖 smoke 通过。
- 统一跨版本验证命令处理真实 GB、SK 样本，均走完整 OOXML 包快速等价路径，`mismatch_count = 0`。
- 实际运行计数保持不变：
  - GB：输入 57,908 行，数量/工单分析各 1,124 行，`error_log_count = 20,515`。
  - SK：输入 467,420 行，数量/工单分析各 11,239 行，`error_log_count = 201,815`。

## 剩余风险

该阶段尚不代表 v0.3.0 已发布；版本、当前事实文档、发布 workflow、性能和 Windows 包仍需在后续阶段完成最终验收。

## 恢复

删除内容可从 Git 标签 `cleanup-baseline-2026-07-27` 恢复；当前分支不保留第二套退役实现。
