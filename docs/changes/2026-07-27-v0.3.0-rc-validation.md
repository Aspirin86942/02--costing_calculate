# v0.3.0 RC 最终验收

- 日期：2026-07-27
- RC 标签：`v0.3.0-rc.1`
- RC 源提交：`df7021aaebdc8f33960678d9ff3171b2ddab697d`
- 状态：通过

## 质量门禁

- Rust fmt 通过。
- Rust 全 workspace、全 target、全 feature Clippy 通过，warnings 视为错误。
- Rust 全 workspace、全 feature 测试通过：209 passed。
- uv frozen 环境同步通过。
- Python 验证工具 Ruff lint / format 通过。
- 精简后的 Python 公共测试通过：63 passed。
- 当前 Markdown 本地链接检查通过。
- 合成 GB/SK 配置、check-only、正式 workbook 和禁止覆盖 smoke 通过。
- 确定性 build identity 验证通过：应用版本 `0.3.0`，Manifest schema 仍为 V1。

## 真实数据等价

冻结基线 binary 与 RC binary 分别处理同一份真实输入，报告不记录文件名、绝对路径或单元格值。

| Pipeline | 模式 | 输入行 | 分析行 | error log | 比较 |
|---|---|---:|---:|---:|---|
| GB | normal | 57,908 | 1,124 | 20,515 | OOXML package fast path，0 mismatch |
| SK | normal | 467,420 | 11,239 | 201,815 | OOXML package fast path，0 mismatch |
| GB | 单月过滤 | 57,908 | 85 | 1,392 | OOXML package fast path，0 mismatch |
| SK | 单月过滤 | 467,420 | 305 | 5,744 | OOXML package fast path，0 mismatch |

package fast path 会逐成员比较完整 ZIP，只忽略正常变化的 `docProps/core.xml`。若不相同才进入语义比较，因此这次结果强于仅比较业务单元格。

GB、SK 真实 check-only 均通过且不写 workbook。GB check-only 同时写出并检查了脱敏 Manifest：

- `schema_version = 1`
- `application.version = 0.3.0`
- `execution.mode = check-only`
- `result.output_written = false`
- 输入 SHA-256 为 64 位小写十六进制
- 仓库内输入路径只记录为相对路径

数值比较仍使用单元格 `1e-9`、列累计 `1e-8`，没有放宽。

## N=5 性能与内存

正式测量使用 release profile、真实输入、normal mode、独立进程和五个独立输出路径。

| Pipeline | Wall 中位数 | PWS 中位数 | 最大输出 | 结果 |
|---|---:|---:|---:|---|
| GB | 1.8802s | 357,294,080 bytes | 3,808,077 bytes | 通过 |
| SK | 17.1243s | 1,460,334,592 bytes | 43,611,045 bytes | 通过 |

对应硬门禁：

- GB：3.2554s / 375,700,685 bytes / 4,194,321 bytes
- SK：20.0s / 2,147,483,648 bytes / 48,658,823 bytes

本次没有发现性能瓶颈，因此没有做额外优化，也没有启动 8 对优化实验。

## Windows RC 包

从干净 RC 源提交构建：

- ZIP：`dist/costing-calculate-v0.3.0-rc.1-windows-x86_64.zip`
- ZIP SHA-256：`116a8eef63d04481ad3cdc0cd035cd029aae898988914b4b68454c491694e7b5`
- executable SHA-256：`333fc74f653fca60d686ab95b0a86d75eb59746bb4c55938944763f66024a79c`

隔离 smoke 通过：

- ZIP 外部校验和与包内 `SHA256SUMS`；
- 固定目录布局；
- help、version-json 和配置校验；
- GB/SK synthetic check-only；
- GB/SK 正常 workbook 与 Manifest；
- child `PATH` 只保留 Windows System32，不含 Rust 或 Python。

## 仓库与安全

- 敏感 ERP 输入、真实输出 workbook、原始报告和本机路径只保存在忽略目录。
- `docs/superpowers/` tree hash 保持 `ca53d082deb5aaa345cd6cd4fadd57ea3c1160b9`。
- 退役实现仍可从 `cleanup-baseline-2026-07-27` 恢复。
- 本地 RC 标签已创建，但没有推送；没有创建或发布 GitHub Release。

## 发布确认点

v0.3.0 正式说明和发布流程已准备好。推送 RC/正式标签或发布任何外部资产前，仍需用户明确确认。
