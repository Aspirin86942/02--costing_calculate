# v0.2.0 M3 Configuration Governance — 2026-07-25

## Scope and decision

M3 replaces the Rust production path's static product-order arrays with a
versioned, fail-closed `FileConfigV1`. The CLI application layer owns parsing,
validation, source precedence and hashing; `costing-core` receives only an
owned, validated `PipelineRules`.

The reviewed implementation commit is
`c0165c1bf3f27741a1975b6b5904ac57d91842a2`.

The repository owner approved the reviewed production dependencies on
2026-07-25. The exact versions, licenses, checksums, maintenance status and
lockfile delta are recorded in
`docs/dependencies/2026-07-25-toml-sha2-review.md`.

## Frozen artifacts

| Artifact | Bytes | SHA-256 |
| --- | ---: | --- |
| Replayed M0 baseline executable | 3,292,160 | `7e1953fcf3ab3604cbea17ca3652767557de105be31ffeb20b981eaf77365697` |
| Reviewed M3 candidate executable | 3,597,824 | `2d7e7170a97ff80f497c0d5bdaf8a01a6eeb235154ffc7b4ea363ab12da0ca44` |
| GB approved input | 4,350,993 | `6aa5e3e7fdc547ebaaef968eb5b95d4d630c4ec9915184f94346f60687b8e7ee` |
| SK approved input | 55,181,339 | `6eac3c6c9ea0eb3e98ca11fb3829914be63e932595b3e3c613f0da46b385d64f` |
| `costing.default.toml` | 1,775 | `c7037f13dbc5d5b23f74c1214542e955cbe3e3c5699edc2becf656a147ca6f68` |
| `costing.schema.json` | 2,756 | `c933c36fc42e991ed30c1235e9c706ca3c1194578ed3ac7bdf9d38829cd409c0` |

Raw ERP workbook names and paths are intentionally omitted.

## Effective configuration identities

| Pipeline | Embedded `effective_sha256` | External `effective_sha256` | External `source_sha256` |
| --- | --- | --- | --- |
| GB | `70dfecbe8ddd15da9372b6ca54ff15ab4bfad66f068511827e11b48b0288a658` | same | `c7037f13dbc5d5b23f74c1214542e955cbe3e3c5699edc2becf656a147ca6f68` |
| SK | `4d30427de115a88c2e0006c72f05d1d8212e04aaf79e24045d22d031609057ac` | same | `c7037f13dbc5d5b23f74c1214542e955cbe3e3c5699edc2becf656a147ca6f68` |

The semantic hash excludes source labels, paths and TOML layout. The external
source hash covers the complete reviewed TOML bytes.

## Real-data migration comparison

The same approved GB and SK inputs were processed by:

1. the executable rebuilt from the M0 source commit;
2. the M3 executable with its embedded configuration;
3. the M3 executable with the reviewed TOML supplied via `--config`.

| Pipeline | Variant | Output bytes | Error-log rows | Reader rows | Quantity rows | Work-order rows |
| --- | --- | ---: | ---: | ---: | ---: | ---: |
| GB | M0 baseline | 3,808,077 | 20,515 | 57,908 | 1,124 | 1,124 |
| GB | M3 embedded | 3,808,077 | 20,515 | 57,908 | 1,124 | 1,124 |
| GB | M3 external | 3,808,077 | 20,515 | 57,908 | 1,124 | 1,124 |
| GB | Reviewed M3 embedded | 3,808,077 | 20,515 | 57,908 | 1,124 | 1,124 |
| SK | M0 baseline | 43,611,044 | 201,815 | 467,420 | 11,239 | 11,239 |
| SK | M3 embedded | 43,611,044 | 201,815 | 467,420 | 11,239 | 11,239 |
| SK | M3 external | 43,611,044 | 201,815 | 467,420 | 11,239 | 11,239 |
| SK | Reviewed M3 embedded | 43,611,045 | 201,815 | 467,420 | 11,239 | 11,239 |

All six pairwise comparisons required by the three-way migration proof passed
with zero mismatches:

- M0 baseline versus M3 embedded;
- M0 baseline versus M3 external;
- M3 embedded versus M3 external;
- each comparison was performed for both GB and SK.

After the two-axis code review, the release binary was rebuilt from
`c0165c1`. A further M0-baseline versus reviewed-candidate comparison passed
for both GB and SK with zero mismatches. The SK package varied by one byte
because of generated ZIP/document metadata; every semantic and package
contract checked by the oracle remained equal.

The repository's `workbook_compare` oracle checked Sheet order, package
relationships, shapes, panes, filters, widths, number formats, styles,
conditional formats, every cell value and business reconciliation. Raw XLSX
SHA-256 values differ because each run writes a different document metadata
timestamp; this is not a business or workbook-contract difference.

The three variants also reported identical quality metrics, issue-type counts
and runtime counts. GB reported `MISSING_AMOUNT=17,193` and
`NON_POSITIVE_UNIT_COST=3,322`; SK reported `MISSING_AMOUNT=168,424` and
`NON_POSITIVE_UNIT_COST=33,391`.

## Failure and boundary proofs

- `--validate-config` and `--print-effective-config` complete without opening
  a workbook.
- Unknown fields, missing pipelines, invalid UTF-8, invalid TOML types,
  duplicate product identities/orders and unsafe input patterns fail before
  input discovery.
- GB and SK standalone cost items accept only their exact frozen sets and
  order.
- External whitespace, comments and key ordering do not change the effective
  semantic hash.
- `costing-core` contains no TOML parser, filesystem/config-path read,
  environment read, input-discovery glob or CLI argument dependency.
- Configuration diagnostics conflict with `--version-json`; version output
  cannot silently bypass a requested validation.
- The published schema and runtime validator agree on case-insensitive XLSX
  suffixes and unsafe basename patterns.
- Configuration failures retain only the source basename in public error
  details, never its absolute parent path.
- The synthetic embedded/external workbook test is byte-identical; the real
  three-way oracle proof above is semantically identical.

## Verification gates

| Gate | Result |
| --- | --- |
| Rust workspace, locked, all features | 186 passed |
| M3 Rust configuration integration tests | 10 passed |
| Python public fast suite | 252 passed, 684 deselected |
| Python configuration architecture tests | 4 passed |
| `cargo clippy --workspace --all-targets --all-features -- -D warnings` | Passed |
| `cargo fmt --all --check` | Passed |
| Ruff check and format check | Passed |
| Deterministic release build identity | Passed |
| Synthetic GB/SK config, check-only, workbook and no-overwrite smoke | Passed |
| Real GB/SK three-way workbook oracle | 6 initial comparisons plus 2 reviewed-candidate comparisons passed, zero mismatches |

M3 exit conditions are satisfied: embedded and equivalent external
configuration produce zero business difference, ordinary configuration cannot
change sealed contracts, and configuration failures precede workbook I/O.
