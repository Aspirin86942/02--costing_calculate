# Superpowers 历史档案（只读）

本目录保存 2026-07-13 删除前最后快照中的历史计划与设计，仅用于追溯背景。

## 只读边界

- 禁止新增、修改、移动或删除本目录内文件；只有用户明确要求变更档案本身时才可例外。
- 本目录中的状态、路径、命令、依赖和验收项可能已经过时。
- 本目录不得作为当前待办、实现入口或行为真值。
- 新计划写入 `../plans/`，实际变更写入 `../changes/`，重要决策写入 `../decisions/`。
- 当前口径以实际代码与测试、根 `README.md`、`AGENTS.md` 和 `docs/README.md` 为准。
